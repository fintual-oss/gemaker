# frozen_string_literal: true

require 'rails_helper'

RSpec.describe "<%= config.gem_name %> engine" do
  it "deleted this file and added valid specs" do
    pending "AGREGUE LOS TESTS, RESPETE LA NORMAAA"
  end
end
